{% extends 'admin/index.html.twig' %}

{% block title %}Modifier un résultat{% endblock %}

{% block body %}
<h1>Modifier le résultat du match</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('admin_resultats') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}

{% extends 'admin/index.html.twig' %}

{% block body %}
<h1>Modifier une statistique</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('admin_statistiques') }}">Retour à la liste</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle statistique{% endblock %}

{% block body %}
    <h1>Créer une nouvelle statistique</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_statistiques') }}">Retour à la liste des statistiques</a>
{% endblock %}

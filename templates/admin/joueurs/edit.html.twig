{% extends 'base.html.twig' %}

{% block title %}Modifier un joueur{% endblock %}

{% block body %}
    <h1>Modifier le joueur {{ joueur.nom }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_joueurs') }}">Retour à la liste des joueurs</a>
{% endblock %}

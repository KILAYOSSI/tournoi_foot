{% extends 'base.html.twig' %}

{% block title %}Cartons{% endblock %}

{% block body %}
<h1>Liste des cartons liés aux statistiques des matchs</h1>

<table class="table table-bordered table-hover rounded-4 shadow-sm">
    <thead class="table-primary text-center fs-5 fw-bold" style="font-family: 'Georgia', serif;">
        <tr>
            <th>Joueur</th>
            <th>Club du joueur</th>
            <th>Cartons Jaunes 🟨</th>
            <th>Cartons Rouges 🟥</th>
        </tr>
    </thead>
    <tbody>
        {% for stat in cartonsStats %}
            <tr class="text-center" style="font-family: 'Georgia', serif; font-size: 1.1rem;">
                <td class="text-start">{{ stat.joueur.nom }}</td>
                <td>
                    {% if stat.joueur and stat.joueur.club %}
                        {{ stat.joueur.club.nom }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td>{{ stat.cartonsjaunes }}</td>
                <td>{{ stat.cartonsrouges }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucune statistique trouvée.</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Modifier une statistique{% endblock %}

{% block body %}
    <h1>Modifier la statistique {{ statistique.id }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_statistiques') }}">Retour à la liste des statistiques</a>
{% endblock %}
